@model basics.Areas.Admin.Models.DashboardViewModel
@{
    ViewData["Title"] = "Kontrol Paneli";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<style>
    .stat-card {
        border-left: 4px solid;
        transition: transform 0.2s, box-shadow 0.2s;
    }
    .stat-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 25px rgba(0,0,0,0.1) !important;
    }
    .stat-icon {
        width: 60px;
        height: 60px;
        border-radius: 12px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.5rem;
    }
    .activity-item {
        border-left: 3px solid #e9ecef;
        transition: all 0.2s;
    }
    .activity-item:hover {
        border-left-color: #0d6efd;
        background-color: #f8f9fa;
    }
    .progress-thin {
        height: 6px;
    }
</style>

<div class="container-fluid p-4">
    
    <!-- WELCOME SECTION -->
    <div class="row mb-4">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center">
                <div>
                    <h2 class="fw-bold mb-1">
                        Hoş Geldiniz, @User.Identity.Name! 👋
                    </h2>
                    <p class="text-muted mb-0">İşte bugünkü özet bilgileriniz</p>
                </div>
                <div class="text-muted">
                    <i class="fa-solid fa-calendar me-2"></i>@DateTime.Now.ToString("dd MMMM yyyy, dddd")
                </div>
            </div>
        </div>
    </div>

    <!-- STATISTICS CARDS -->
    <div class="row g-4 mb-4">
        <!-- Toplam Etkinlikler -->
        <div class="col-md-6 col-lg-3">
            <div class="card border-0 shadow-sm stat-card" style="border-left-color: #0d6efd;">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <p class="text-muted text-uppercase small fw-bold mb-1">Toplam Etkinlik</p>
                            <h3 class="fw-bold mb-0">@Model.ToplamEtkinlik</h3>
                        </div>
                        <div class="stat-icon bg-primary bg-opacity-10 text-primary">
                            <i class="fa-solid fa-masks-theater"></i>
                        </div>
                    </div>
                    <div class="mt-3">
                        <small class="text-success">
                            <i class="fa-solid fa-arrow-up me-1"></i>@Model.AktifEtkinlik Aktif
                        </small>
                    </div>
                </div>
            </div>
        </div>

        <!-- Satılan Biletler -->
        <div class="col-md-6 col-lg-3">
            <div class="card border-0 shadow-sm stat-card" style="border-left-color: #198754;">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <p class="text-muted text-uppercase small fw-bold mb-1">Satılan Bilet</p>
                            <h3 class="fw-bold mb-0">@Model.ToplamSatilanBilet</h3>
                        </div>
                        <div class="stat-icon bg-success bg-opacity-10 text-success">
                            <i class="fa-solid fa-ticket"></i>
                        </div>
                    </div>
                    <div class="mt-3">
                        <small class="text-muted">
                            <i class="fa-solid fa-chart-line me-1"></i>Tüm zamanlar
                        </small>
                    </div>
                </div>
            </div>
        </div>

        <!-- Toplam Hasılat - Sadece Admin ve Okan -->
        @if (User.IsInRole("Admin") || User.IsInRole("Okan"))
        {
            <div class="col-md-6 col-lg-3">
                <div class="card border-0 shadow-sm stat-card" style="border-left-color: #ffc107;">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-center">
                            <div>
                                <p class="text-muted text-uppercase small fw-bold mb-1">Toplam Hasılat</p>
                                <h3 class="fw-bold mb-0">@Model.ToplamHasilat.ToString("C0")</h3>
                            </div>
                            <div class="stat-icon bg-warning bg-opacity-10 text-warning">
                                <i class="fa-solid fa-money-bill-wave"></i>
                            </div>
                        </div>
                        <div class="mt-3">
                            <small class="text-muted">
                                <i class="fa-solid fa-piggy-bank me-1"></i>TL
                            </small>
                        </div>
                    </div>
                </div>
            </div>
        }

        <!-- Salonlar - Sadece Admin -->
        @if (User.IsInRole("Admin"))
        {
            <div class="col-md-6 col-lg-3">
                <div class="card border-0 shadow-sm stat-card" style="border-left-color: #6c757d;">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-center">
                            <div>
                                <p class="text-muted text-uppercase small fw-bold mb-1">Toplam Salon</p>
                                <h3 class="fw-bold mb-0">@Model.ToplamSalon</h3>
                            </div>
                            <div class="stat-icon bg-secondary bg-opacity-10 text-secondary">
                                <i class="fa-solid fa-building"></i>
                            </div>
                        </div>
                        <div class="mt-3">
                            <small class="text-muted">
                                <i class="fa-solid fa-couch me-1"></i>Kayıtlı
                            </small>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>

    <div class="row g-4">
        <!-- YAKLAŞAN ETKİNLİKLER -->
        <div class="col-lg-8">
            <div class="card border-0 shadow-sm h-100">
                <div class="card-header bg-white py-3 d-flex justify-content-between align-items-center">
                    <h5 class="m-0 fw-bold">
                        <i class="fa-solid fa-calendar-days me-2 text-primary"></i>Yaklaşan Etkinlikler
                    </h5>
                    <a asp-controller="Etkinlikler" asp-action="Index" class="btn btn-sm btn-outline-primary">
                        Tümünü Gör <i class="fa-solid fa-arrow-right ms-1"></i>
                    </a>
                </div>
                <div class="card-body p-0">
                    @if (Model.YakinEtkinlikler.Any())
                    {
                        <div class="list-group list-group-flush">
                            @foreach (var etkinlik in Model.YakinEtkinlikler)
                            {
                                <div class="list-group-item activity-item">
                                    <div class="d-flex justify-content-between align-items-start">
                                        <div class="flex-grow-1">
                                            <div class="d-flex align-items-center mb-2">
                                                <h6 class="mb-0 fw-bold">@etkinlik.EtkinlikAdi</h6>
                                                <span class="badge bg-primary ms-2">@etkinlik.DolulukOrani%</span>
                                            </div>
                                            <div class="small text-muted mb-2">
                                                <i class="fa-solid fa-location-dot me-1"></i>@etkinlik.SalonAdi
                                                <span class="mx-2">•</span>
                                                <i class="fa-solid fa-calendar me-1"></i>@etkinlik.TarihSaat.ToString("dd MMM yyyy, HH:mm")
                                            </div>
                                            <div class="progress progress-thin">
                                                <div class="progress-bar bg-primary" role="progressbar" 
                                                     style="width: @etkinlik.DolulukOrani%" 
                                                     aria-valuenow="@etkinlik.DolulukOrani" aria-valuemin="0" aria-valuemax="100">
                                                </div>
                                            </div>
                                            <small class="text-muted mt-1 d-block">
                                                @etkinlik.SatilanBilet / @etkinlik.ToplamKapasite koltuk satıldı
                                            </small>
                                        </div>
                                        <div class="ms-3">
                                            <a asp-controller="Satislar" asp-action="SatisYap" asp-route-id="@etkinlik.Id" 
                                               class="btn btn-sm btn-outline-success">
                                                <i class="fa-solid fa-cash-register"></i>
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            }
                        </div>
                    }
                    else
                    {
                        <div class="p-5 text-center text-muted">
                            <i class="fa-solid fa-calendar-xmark fa-3x mb-3 opacity-25"></i>
                            <p class="mb-0">Yaklaşan etkinlik bulunmuyor</p>
                        </div>
                    }
                </div>
            </div>
        </div>

        <!-- SON SATIŞLAR & HIZLI İŞLEMLER -->
        <div class="col-lg-4">
            <!-- HIZLI İŞLEMLER -->
            <div class="card border-0 shadow-sm mb-4">
                <div class="card-header bg-primary text-white py-3">
                    <h6 class="m-0 fw-bold">
                        <i class="fa-solid fa-bolt me-2"></i>Hızlı İşlemler
                    </h6>
                </div>
                <div class="card-body p-2">
                    <div class="d-grid gap-2">
                        <a asp-area="Admin" asp-controller="Etkinlikler" asp-action="Index" class="btn btn-outline-primary btn-sm text-start">
                            <i class="fa-solid fa-plus me-2"></i>Yeni Etkinlik Ekle
                        </a>
                        <a asp-area="Admin" asp-controller="Satislar" asp-action="Index" class="btn btn-outline-success btn-sm text-start">
                            <i class="fa-solid fa-ticket me-2"></i>Bilet Sat
                        </a>
                        @if (User.IsInRole("Admin") || User.IsInRole("Okan"))
                        {
                            <a asp-area="Admin" asp-controller="Rapor" asp-action="Index" class="btn btn-outline-info btn-sm text-start">
                                <i class="fa-solid fa-chart-line me-2"></i>Raporları Görüntüle
                            </a>
                        }
                    </div>
                </div>
            </div>

            <!-- SON SATIŞLAR -->
            <div class="card border-0 shadow-sm">
                <div class="card-header bg-white py-3">
                    <h6 class="m-0 fw-bold">
                        <i class="fa-solid fa-clock-rotate-left me-2 text-success"></i>Son Satışlar
                    </h6>
                </div>
                <div class="card-body p-0" style="max-height: 400px; overflow-y: auto;">
                    @if (Model.SonSatislar.Any())
                    {
                        <div class="list-group list-group-flush">
                            @foreach (var satis in Model.SonSatislar)
                            {
                                <div class="list-group-item px-3 py-2">
                                    <div class="d-flex justify-content-between align-items-start">
                                        <div class="small flex-grow-1">
                                            <div class="fw-bold text-truncate">@satis.EtkinlikAdi</div>
                                            <div class="text-muted" style="font-size: 0.75rem;">
                                                @satis.KoltukNo • @satis.MusteriAdi
                                            </div>
                                            <div class="text-muted" style="font-size: 0.7rem;">
                                                @satis.SatisTarihi.ToString("dd/MM HH:mm")
                                                @if (!string.IsNullOrEmpty(satis.SatisPlatformu))
                                                {
                                                    <span class="badge badge-sm @(satis.SatisPlatformu == "Bubilet" ? "bg-success" : satis.SatisPlatformu == "Biletinial" ? "bg-warning" : "bg-secondary")">
                                                        @satis.SatisPlatformu
                                                    </span>
                                                }
                                            </div>
                                        </div>
                                        <span class="badge bg-success ms-2">@satis.Fiyat.ToString("C0")</span>
                                    </div>
                                </div>
                            }
                        </div>
                    }
                    else
                    {
                        <div class="p-4 text-center text-muted">
                            <i class="fa-solid fa-inbox fa-2x mb-2 opacity-25"></i>
                            <p class="small mb-0">Henüz satış yok</p>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>

</div>